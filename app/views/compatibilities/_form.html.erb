<%= form_for(@compatibility) do |f| %>
<div style="padding-top: 50px; text-align: center">
  <% if @compatibility.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@compatibility.errors.count, "error") %> prohibited this compatibility from being saved:</h2>

      <ul>
      <% @compatibility.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div style="padding: 0 10px;">
	  <table style="margin-top: 5px;width: 100%; padding: 20px; border:1px #F3D407 solid; border-radius:5px; -moz-border-radius:3px; -webkit-border-radius:3px; font-size: 12px;">
	  	<tr>
	  		<td colspan="2" style="">
	  			<span style="font-size:26px;">반려동물 궁합보기</span>
	  			<span style="margin-top:10px; font-size:12px; display: block;">
	  				반려동물과 본인의 이름을 입력하면,<br />
	  				전생을 통해 서로의 궁합을 알려드립니다.<br />
	  			</span>
	  			<span style="font-size:12px; color: gray;">(재미 위주로 봐주세요~!^^)</span>
	  			
	  		</td>
	  	</tr>
	  	<tr>
	  		<td style="text-align: right; padding-top: 10px;">본인이름: </td>
	  		<%= f.hidden_field :uniq_key %>
	  		<td style="text-align: left; padding-top: 10px;"><%= f.text_field :owner_name %></td>
	  	</tr>
	  	<tr>
	  		<td style="text-align: right; padding-top: 10px;">반려동물 이름:</td>
	  		<td style="text-align: left; padding-top: 10px;"><%= f.text_field :animal_name %></td>
	  	</tr>
	  	<tr>
	  		<td colspan="2" style="padding-top: 10px;"><%= f.submit "궁합 보기" %></td>
	  	</tr>
	  </table>
	  <div style="text-align: right; margin-top: 10px;">
		<a id="kakao-link-btn" href="javascript:;">
	    	<img style="width: 50px;" src="http://dn.api1.kage.kakao.co.kr/14/dn/btqa9B90G1b/GESkkYjKCwJdYOkLvIBKZ0/o.jpg" />
	    </a>
	  </div>
  </div>
</div>
<% end %>

<div style="padding-top: 10px;">
	<hr style="border-bottom: 0; color: gray; margin:0;" />
	<div style="display: center; width: 100%;">
		<% @feeds.each do |feed| %>
		<a name="<%= feed.id %>">
			<div style="padding: 10px;">
				<table>
					<tr>
						<!-- <td style="width: 300px; height: 300px; border-radius: 150px; -webkit-border-radius: 150px; -moz-border-radius: 150px; background: url(http://link-to-your/image.jpg) no-repeat;">
							
						</td> -->
						<td rowspan="2" style="padding: 0;width: 50px; height: 50px; background-position: center; background-repeat: no-repeat; overflow: hidden;" >
							<img style="width:50px; height: 50px; border-radius: 25px; -webkit-border-radius: 25px; -moz-border-radius: 25px; no-repeat;" src="<%=feed.user.user_cover.image.url%>" />
						</td>
						<td style="padding: 0;"><%= feed.user.user_cover.name %></td>
					</tr>
					<tr>
						<td style="padding: 0;"><%= @time_word[feed.id] %></td>
					</tr>
				</table>
			</div>
			<div style="margin-top: -4px;"><img style="width:100%" src="<%=feed.feed_photos[0].image.url%>" /></div>
			<div style="padding: 20px 10px; margin-top:-4px;"><%= feed.content %></div>
			<hr style="border-bottom: 0; color: gray; margin:0;" />
		</a>
		<% end %>
	</div>
</div>

<script>
	cname = "uniq_key";
	if(document.cookie == ""){
	    var expire = new Date();
	    expire.setDate(expire.getDate() + 1000);
	    cookies = cname + '=' + getUUID() + '; path=/ '; 
	    cookies += ';expires=' + expire.toGMTString() + ';';
		document.cookie = cookies;
	}
	$("#play_uniq_key").val(getCookie());

	function getUUID() {
		return 'xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
		var r = Math.random()*16|0, v = c == 'x' ? r : (r&0x3|0x8);
		return v.toString(16);
		});
	}
	
	function getCookie() {
	    var name = cname + "=";
	    var ca = document.cookie.split(';');
	    for(var i=0; i<ca.length; i++) {
	        var c = ca[i];
	        while (c.charAt(0)==' ') c = c.substring(1);
	        if (c.indexOf(name) == 0) return c.substring(name.length,c.length);
	    }
	    return "";
	}
</script>
<script>
    // 사용할 앱의 JavaScript 키를 설정해 주세요.
    Kakao.init('db06d124c2ef3f283fedea4c9ab19959');

    // 카카오톡 링크 버튼을 생성합니다. 처음 한번만 호출하면 됩니다.
    Kakao.Link.createTalkLinkButton({
      container: '#kakao-link-btn',
      label: '내 애완동물과의 궁합결과 보기',
      image: {
        src: 'http://dn.api1.kage.kakao.co.kr/14/dn/btqaWmFftyx/tBbQPH764Maw2R6IBhXd6K/o.jpg',
        width: '300',
        height: '200'
      },
      webButton: {
        text: '카카오 디벨로퍼스',
        url: 'https://dev.kakao.com/docs/js' // 앱 설정의 웹 플랫폼에 등록한 도메인의 URL이어야 합니다.
      }
    });
</script>