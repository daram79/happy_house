<%= form_for(@play_answer) do |f| %>
  <% if @play_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@play_answer.errors.count, "error") %> prohibited this play_answer from being saved:</h2>

      <ul>
      <% @play_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
