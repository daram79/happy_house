<%= form_for(@user_cover) do |f| %>
  <% if @user_cover.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_cover.errors.count, "error") %> prohibited this user_cover from being saved:</h2>

      <ul>
      <% @user_cover.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
